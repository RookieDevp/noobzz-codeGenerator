# Tomcat
server:
  port: 9301

# Spring
spring: 
  application:
    # 应用名称
    name: noobzz-gen
  profiles:
    # 环境配置
    active: prod
  jackson:
    time-zone: GMT+8
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 175.178.240.180:8848
      config:
        # 配置中心地址
        server-addr: 175.178.240.180:8848
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}

